package sif.model.values;

import sif.model.Cell;

/**
 * Enumerates the different types of return value of a {@link Cell}.
 */
public enum ValueType {
    BLANK, BOOLEAN, ERROR, NUMERIC, STRING, TEXT
}